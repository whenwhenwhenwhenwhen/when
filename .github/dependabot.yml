version: 2

updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
    open-pull-requests-limit: 5
    # Let a release age on the registry before adopting it. A version that is
    # unpublished, hot-patched, or pulled for a supply-chain issue is usually
    # caught within hours, and none of these updates are urgent enough to
    # justify being the first consumer.
    cooldown:
      default-days: 1
      semver-patch-days: 1
      semver-minor-days: 3
      semver-major-days: 7
    groups:
      react:
        patterns:
          - react
          - react-dom
          - react-router
      types:
        patterns:
          - "@types/*"
      dev-dependencies:
        dependency-type: development
        exclude-patterns:
          - "@types/*"
          - typescript
    ignore:
      # TypeScript does not follow semver for type checking: a minor release can
      # legitimately introduce new errors in unchanged code. Majors are adopted
      # deliberately, with a real build, rather than by bot.
      - dependency-name: typescript
        update-types:
          - version-update:semver-major

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
    cooldown:
      default-days: 3
    groups:
      actions:
        patterns:
          - "*"

  - package-ecosystem: docker
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
    cooldown:
      default-days: 3
